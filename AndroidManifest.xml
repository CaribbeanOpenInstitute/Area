<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jm.org.data.era"
    android:versionCode="1"
    android:versionName="1.0" >

    <supports-screens
        android:normalScreens="false"
        android:smallScreens="false" />

    <uses-sdk
        android:minSdkVersion="11"
        android:targetSdkVersion="21" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name="jm.org.data.era.AreaApplication"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" 
        android:debuggable="true">
        <activity
            android:name="jm.org.data.era.HomeActivity"
            android:label="@string/app_name"
            android:screenOrientation="sensorLandscape" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- This metadata entry causes .SearchableActivity to be the default context -->
            <!-- whenever the user invokes search while in this Activity. -->
            <meta-data
                android:name="android.app.default_searchable"
                android:value=".SearchableActivity" />
        </activity>
        <activity
            android:name="jm.org.data.era.SearchableActivity"
            android:screenOrientation="sensorLandscape" >
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        
        <activity android:name="org.achartengine.GraphicalActivity" />
        
        <activity
            android:name="SavedDataActivity"
            android:screenOrientation="sensorLandscape" >
            <!-- This metadata entry causes .SearchableActivity to be the default context -->
            <!-- whenever the user invokes search while in this Activity. -->
            <meta-data
                android:name="android.app.default_searchable"
                android:value=".SearchableActivity" />
        </activity>
        
        <activity android:name="jm.org.data.era.AreaPreferencesActivity" />
        <activity
            android:name="jm.org.data.era.IndicatorActivity"
            android:screenOrientation="sensorLandscape">
            <!-- This metadata entry causes .SearchableActivity to be the default context -->
            <!-- whenever the user invokes search while in this Activity. -->
            <meta-data
                android:name="android.app.default_searchable"
                android:value=".SearchableActivity" />
        </activity>
        <activity
            android:name="jm.org.data.era.StartupActivity"
            android:screenOrientation="sensorLandscape"
            android:theme="@style/DialogTheme" />
        
        <activity android:name="jm.org.data.era.AreaActivity" />
        
        <activity
            android:name="jm.org.data.era.ArticleViewActivity"
            android:screenOrientation="sensorLandscape" >
            <meta-data
                android:name="android.app.default_searchable"
                android:value=".SearchableActivity" />
        </activity>
        
        <activity
            android:name="jm.org.data.era.ReportDetailViewActivity"
            android:screenOrientation="sensorLandscape" >
            <meta-data
                android:name="android.app.default_searchable"
                android:value=".SearchableActivity" />
        </activity>
        
        <activity
            android:name="jm.org.data.era.ExpandableList2"
            android:screenOrientation="sensorLandscape" >
            <meta-data
                android:name="android.app.default_searchable"
                android:value=".SearchableActivity" />
        </activity>
        
        <activity
            android:name="jm.org.data.era.CollectionsActivity"
            android:screenOrientation="sensorLandscape" >
            
            <meta-data
                android:name="android.app.default_searchable"
                android:value=".SearchableActivity" />
        </activity>
        
        <activity
            android:name="jm.org.data.era.CountryActivity"
            android:screenOrientation="sensorLandscape" >
            <meta-data
                android:name="android.app.default_searchable"
                android:value=".SearchableActivity" />
        </activity>
        <activity
            android:name="jm.org.data.era.ReportWebViewActivity"
            android:screenOrientation="sensorLandscape" >
            <meta-data
                android:name="android.app.default_searchable"
                android:value=".SearchableActivity" />
        </activity>

        <service
            android:name="AreaService"
            android:icon="@drawable/ic_launcher"
            android:label="@string/areaService" />
        
        <provider 
            android:name="jm.org.data.era.AreaProvider"
            android:authorities="jm.org.data.era.AreaProvider"
            android:multiprocess="true" />
            
        

        <!-- android:process=":AreaService" -->

    </application>

</manifest>